import type { Response } from 'express';
import type { AuthRequest } from '../types/auth.types.js';

export const followUser = (req: AuthRequest, res: Response) => {
  try {
  } catch (error) { }
};

export const unfollowUser = (req: AuthRequest, res: Response) => {
  try {
  } catch (error) { }
};

export const getFollowers = (req: AuthRequest, res: Response) => {
  try {
  } catch (error) { }
};

export const getFollowing = (req: AuthRequest, res: Response) => {
  try {
  } catch (error) { }
};