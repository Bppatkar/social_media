version: '3.9'

services:
  client:
    build:
      context: ./client
    ports:
      - '3000:3000'
    env_file:
      - ./client/.env.local
    depends_on:
      - server

  server:
    build:
      context: ./server
    ports:
      - '8000:8000'
    env_file:
      - ./server/.env
